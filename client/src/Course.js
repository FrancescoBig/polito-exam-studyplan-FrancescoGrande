function Course(code, name, credits, enrstudents, maxstudents, incompatible, preparatory) {

    this.code = code;
    this.name = name;
    this.credits = credits;
    this.enrstudents = enrstudents;
    this.maxstudents = maxstudents;
    this.incompatible = incompatible;
    this.preparatory = preparatory;

}

export default Course;